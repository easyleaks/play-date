import SwiftUI

/// Typography tokens.
/// TODO: Add custom font and text styles if needed.
enum AppTypography {
    static let title = Font.title2
    static let body = Font.body
    static let caption = Font.caption
}
